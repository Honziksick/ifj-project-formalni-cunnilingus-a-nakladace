// Syntax error: Incorrect number of brackets in condition

const ifj = @import("ifj24.zig");

pub fn main() void {
    var x: i32 = 32;
    const y : i32 = 89;

    while(((y))
    {
        processItem(1458242);
    }
}