// Syntax error: Missing "else" keyword

const ifj = @import("ifj24.zig");

pub fn main() void {
    var x: i32 = 32;
    const y : i32 = 89;

    if(y < x)
    {
        processItem(1458242);
    }
    
    {
        _ = handleOddItem(53878);
    }
}