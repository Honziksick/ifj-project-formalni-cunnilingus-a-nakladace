// Syntax error: missing operator in condition

const ifj = @import("ifj24.zig");

pub fn main() void {
    var x: i32 = 32;
    const y : i32 = 89;

    while(y  x)
    {
        processItem(1458242);
    }
}